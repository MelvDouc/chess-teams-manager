import MatchForm from "@components/MatchForm/MatchForm.jsx";

export default function MatchCreatePage() {
  return (
    <MatchForm
      match={null}
      handleSubmit={() => { }}
    ></MatchForm>
  );
}